node_modules
npm-debug.log
.DS_Store
.git
.vscode
*.local
coverage
# Keep default input.csv in the image so scraper can run without upload
# (do NOT ignore src/input.csv)
output.csv
# Keep .env included so env vars are baked into the image